# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - asp-fastapi-docker-actions-lab

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read #This is required for actions/checkout

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to container registry
        uses: docker/login-action@v2
        with:
          registry: acrumafastapiactionsunique.azurecr.io/
          username: ${{ secrets.AZUREAPPSERVICE_CONTAINERUSERNAME_C9425533CA1049218C00979DDDC6662B }}
          password: ${{ secrets.AZUREAPPSERVICE_CONTAINERPASSWORD_128B337FD1284C8CA145FF17A2C3F976 }}

      - name: Build and push container image to registry
        uses: docker/build-push-action@v3
        with:
          context: .
          push: true
          tags: acrumafastapiactionsunique.azurecr.io/${{ secrets.AZUREAPPSERVICE_CONTAINERUSERNAME_C9425533CA1049218C00979DDDC6662B }}/fastapi-docker-actions:${{ github.sha }}
          file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write #This is required for requesting the JWT
      contents: read #This is required for actions/checkout

    needs: build
    
    steps:
      
      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_4ACBADCFDEB040859131E4F21AAD701D }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_20FAAEC94E874EE1BCC8C03A4809AF1C }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_09B71CA0BB554E9F9FED6F40622DD7CC }}

      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'asp-fastapi-docker-actions-lab'
          slot-name: 'Production'
          images: 'acrumafastapiactionsunique.azurecr.io/${{ secrets.AZUREAPPSERVICE_CONTAINERUSERNAME_C9425533CA1049218C00979DDDC6662B }}/fastapi-docker-actions:${{ github.sha }}'
          